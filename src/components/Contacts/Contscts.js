import './Contacts.css';

const Contacts = () => {
    return(
        <section id="contacts" className="contacts">
            <h1>my contacts</h1>
        </section>
    );
}

export default Contacts;