import './Contacts.css';

const Contacts = () => {
    return(
        <section id="contacts" className="contacts">
            <h1>telefonchik</h1>
        </section>
    );
}

export default Contacts;